import { styled } from "@mui/material";

export const ContainerMain = styled("main")({
    overflow: "auto",
    display: "flex",
    flex: "1",
    justifyContent: "center",
    padding: "20px 30px",
    textAlign: "justify",
    gridArea: "main"
});
